import feather from 'feather-icons';

// const options = {
// 	'stroke-width': 1.5,
// };

// const icons = Object.values( feather.icons ).map( ( icon ) => {
// 	return {
// 		name: icon.name,
// 		svg: icon.toSvg( options ),
// 	};
// } );

export default { ...feather.icons };
